$volup = wpctl set-mute @DEFAULT_AUDIO_SINK@ 0 && wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%+ -l 2.0 && pw-play --volume=0.2 /usr/share/sounds/freedesktop/stereo/audio-volume-change.oga
$voldown = wpctl set-mute @DEFAULT_AUDIO_SINK@ 0 && wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%- -l 2.0 && pw-play --volume=0.2 /usr/share/sounds/freedesktop/stereo/audio-volume-change.oga
$volmute = wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle && pw-play --volume=0.2 /usr/share/sounds/freedesktop/stereo/audio-volume-change.oga
$lightup = light -A 10 && light -G > $SWAYSOCK.wob
$lightdown = light -U 10 && light -G > $SWAYSOCK.wob
$launcher = tofi-drun --history true --drun-launch=true --prompt-text " "
$sinkswitch = pactl set-default-sink $(pactl list short sinks | awk '{print $2}' | sed '/easyeffects_sink/d' | sed "/$(pactl get-default-sink)/d" | tofi --prompt-text " " --height 118 --width 480 --auto-accept-single true ) && pw-play --volume=0.2 /usr/share/sounds/freedesktop/stereo/audio-volume-change.oga
$micmute = wpctl set-mute @DEFAULT_SOURCE@ toggle ; wpctl get-volume @DEFAULT_SOURCE@ | rg 'MUTED' && notify-send --urgency=low ' Mic Muted' -h string:x-canonical-private-synchronous:mic-state || notify-send --urgency=low ' Mic unmuted' -h string:x-canonical-private-synchronous:mic-state
#submaps
bind  = ALT, a, submap,onehand
bind  = ALT_SHIFT, 0, submap, "Gaming Mode - Press alt+shift+0 to exit"
submap=onehand
bind  = , d, movefocus, r
bind  = , a, movefocus, l
bind  = , w, movefocus, u
bind  = , s, movefocus, d
bind  = ALT, d, movewindow, r
bind  = ALT, a, movewindow, l
bind  = ALT, w, movewindow, u
bind  = ALT, s, movewindow, d
binde = SUPER, d, resizeactive,10 0
binde = SUPER, a, resizeactive,-10 0
binde = SUPER, w, resizeactive,0 -10
binde = SUPER, s, resizeactive,0 10
bindel=,XF86AudioRaiseVolume, exec, $volup
bindel=,XF86AudioLowerVolume, exec, $voldown
bindl = ALT, UP, exec, $volup
bindl = ALT, DOWN, exec, $voldown
bindl =,XF86AudioMute, exec, $volmute
bindl =,XF86MonBrightnessUp, exec, $lightup
bindl =,XF86MonBrightnessDown, exec, $lightdown
bindl =,XF86AudioPlay, exec, playerctl play
bindl =,XF86AudioPause, exec, playerctl pause
bindl =,XF86AudioNext, exec, playerctl next
bindl =,XF86AudioPrev, exec, playerctl previous
bindm = SUPER, mouse:272, movewindow
bindm = SUPER, mouse:273, resizewindow
bind  =,ESCAPE, submap,reset
bindnr=,catchall, submap,reset
submap=reset
submap="Gaming Mode - Press alt+shift+0 to exit"
bind  = ALT_SHIFT, 0, submap, reset
bindel=,XF86AudioRaiseVolume, exec, $volup
bindel=,XF86AudioLowerVolume, exec, $voldown
bindl =,XF86AudioMute, exec, $volmute
bindl =,XF86MonBrightnessUp, exec, $lightup
bindl =,XF86MonBrightnessDown, exec, $lightdown
bindl =,XF86AudioPlay, exec, playerctl play
bindl =,XF86AudioPause, exec, playerctl pause
bindl =,XF86AudioNext, exec, playerctl next
bindl =,XF86AudioPrev, exec, playerctl previous
submap=reset

#close hyprland
bind  = SUPER_ALT, o, exit,

#Window management
bind  = SUPER, d, killactive,
bind  = SUPER, s, togglefloating,
bind  = SUPER, v, togglesplit,
bind  = SUPER_ALT, f, exec, killall -SIGUSR1 waybar
bind  = ALT, f, fullscreen

#Programs
bind  = SUPER, f, exec, alacritty msg create-window || alacritty
bind  = SUPER, e, exec, xdg-open ~
bind  = SUPER, a, exec, hyprctl dispatch closewindow Zim & zim
bind  = SUPER, t, exec, firefox
bind  = CONTROL_SHIFT, escape, exec, resources
bind  = SUPER_ALT, o, exec, syncplay --no-gui

#Menus
bind  = SUPER, SPACE, exec, $launcher
bind  = SUPER, ESCAPE, exec, ~/.config/tofi/scripts/power.sh
bind  =,XF86PowerOff, exec, ~/.config/tofi/scripts/power.sh
bind  = SUPER_ALT, d, exec, alacritty msg create-window --config-file ~/.config/alacritty/alacritty-speedreader.toml --class 'Alacritty_floating,Alacritty_floating' -e bash -c 'wl-paste | speedread -w 600' || alacritty --config-file ~/.config/alacritty/alacritty-speedreader.toml --class 'Alacritty_floating,Alacritty_floating' -e bash -c 'wl-paste | speedread -w 600'

# Move focus with mainMod + arrow keys
bind  = ALT, SEMICOLON, movefocus, r
bind  = ALT, j, movefocus, l
bind  = ALT, k, movefocus, u
bind  = ALT, l, movefocus, d
bind  = SUPER, SEMICOLON, movewindow, r
bind  = SUPER, j, movewindow, l
bind  = SUPER, k, movewindow, u
bind  = SUPER, l, movewindow, d

# Switch workspaces
bind  = ALT, 1, workspace, 1
bind  = ALT, 2, workspace, 2
bind  = ALT, 3, workspace, 3
bind  = ALT, 4, workspace, 4
bind  = ALT, 5, workspace, 5
bind  = ALT, 6, workspace, 6
bind  = ALT, 7, workspace, 7
bind  = ALT, b, workspace, 7
bind  = ALT, 8, workspace, 8
bind  = ALT, c, workspace, 8
bind  = ALT, 9, workspace, 9
bind  = ALT, x, workspace, 9
bind  = ALT, 0, workspace, 10
bind  = ALT, z, workspace, 10
bind  = ALT, r, togglespecialworkspace, 

# Move to workspace
bind  = SUPER, 1, movetoworkspacesilent, 1
bind  = SUPER, 2, movetoworkspacesilent, 2
bind  = SUPER, 3, movetoworkspacesilent, 3
bind  = SUPER, 4, movetoworkspacesilent, 4
bind  = SUPER, 5, movetoworkspacesilent, 5
bind  = SUPER, 6, movetoworkspacesilent, 6
bind  = SUPER, 7, movetoworkspacesilent, 7
bind  = SUPER, b, movetoworkspacesilent, 7
bind  = SUPER, 8, movetoworkspacesilent, 8
bind  = SUPER, c, movetoworkspacesilent, 8
bind  = SUPER, 9, movetoworkspacesilent, 9
bind  = SUPER, x, movetoworkspacesilent, 9
bind  = SUPER, 0, movetoworkspacesilent, 10
bind  = SUPER, z, movetoworkspacesilent, 10
bind  = SUPER, r, movetoworkspacesilent, special:

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = SUPER, mouse:272, movewindow
bindm = SUPER, mouse:273, resizewindow

#volume/brightness
bindel=,XF86AudioRaiseVolume, exec, $volup
bindel=,XF86AudioLowerVolume, exec, $voldown
bindl = ALT, UP, exec, $volup
bindl = ALT, DOWN, exec, $voldown
bindl = ALT, t, exec, $volup
bindl = ALT, g, exec, $voldown
bindl =,XF86AudioMute, exec, $volmute
bindl =,XF86MonBrightnessUp, exec, $lightup
bindl =,XF86MonBrightnessDown, exec, $lightdown
bindl = ALT, q, exec, $sinkswitch
bind  = ALT, v, exec, $micmute

# media
bindl =,XF86AudioPlay, exec, playerctl play
bindl =,XF86AudioPause, exec, playerctl pause
bindl =,XF86AudioNext, exec, playerctl next
bindl =,XF86AudioPrev, exec, playerctl previous

#notifications
bind  = ALT, d, exec, makoctl dismiss -g
bind  = ALT, e, exec, makoctl invoke

# screenshots
bind  = ALT, s, exec, grim -g "$(slurp -b 00000000 -s cc000090 -w 0)" $(xdg-user-dir PICTURES)/.clipboard.png && wl-copy --type image/png < $(xdg-user-dir PICTURES)/.clipboard.png && wl-paste > $(xdg-user-dir PICTURES)/.clipboard/$(date +'%s.png') && pw-play --volume=0.05 /usr/share/sounds/freedesktop/stereo/camera-shutter.oga
bind  = SUPER_ALT, r, exec, grim -o $(swaymsg -t get_outputs | jq -r '.[] | select(.focused) | .name') $(xdg-user-dir PICTURES)/.clipboard.png && wl-copy --type image/png < $(xdg-user-dir PICTURES)/.clipboard.png && wl-paste > $(xdg-user-dir PICTURES)/.clipboard/$(date +'%s.png') && pw-play --volume=0.05 /usr/share/sounds/freedesktop/stereo/camera-shutter.oga
bind  = SUPER_ALT, s, exec, grim -g "$(slurp -b 00000000 -s cc000090 -w 0)" - | tesseract -l "eng" stdin /tmp/tesseract && pw-play --volume=0.05 /usr/share/sounds/freedesktop/stereo/camera-shutter.oga & wl-copy < /tmp/tesseract.txt && rm /tmp/tesseract.txt

# lock screen
bindrl = SUPER_ALT, p, exec, sleep 0.5 && systemctl suspend & swaylock --screenshot --effect-blur 7x5 --effect-vignette 1:1
